## O quê e por quê / What and why

<!-- Descreva a mudança e o motivo. / Describe the change and the reason for it. -->

## Checklist

- [ ] `flutter analyze` e `flutter test` passam localmente / pass locally
- [ ] Strings novas em **pt-BR e en-US** (`app_pt.arb` + `app_en.arb`) / New strings in **both languages**
- [ ] Sem cor crua — só tokens de `FoxColors` / No raw colors — `FoxColors` tokens only
- [ ] Testes para lógica nova, se aplicável / Tests for new logic, if applicable
- [ ] Screenshot/GIF anexado, se a mudança for visual / Screenshot/GIF attached, if the change is visual

<!--
Mudanças maiores (dependência nova, backend de captura, paleta/marca) precisam
de aval prévio do autor — veja SPEC.md §7 antes de abrir o PR.

Larger changes (new dependency, capture backend, palette/brand) need prior
approval — see SPEC.md §7 before opening the PR.
-->
